{
  "name": "processamento-ativos",
  "version": "1.0.0",
  "description": "Lambda TypeScript para processamento de ordens via evento SQS usando ProxySQL como proxy RDS",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "start:poller": "node dist/poller.js",
    "dev:poller": "ts-node-dev --respawn --transpile-only src/poller.ts",
    "local:invoke": "npx ts-node src/local-invoke.ts",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.1039.0",
    "@prisma/client": "^5.10.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.109",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.13.0",
    "jest": "^30.4.2",
    "prisma": "^5.10.0",
    "ts-jest": "^29.4.9",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5",
    "winston": "^3.17.0"
  }
}
